#ifndef PQ_HPP
#define PQ_HPP

#include "utils.hpp"

void pq( double       *       Fattr,
         double       * const Y,
         double const * const p_sp,
         matidx       *       ir,
         matidx       *       jc,
         int    const         n,   
         int    const         d);

#endif
